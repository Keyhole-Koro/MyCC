int max(int a, int b) {
    if(a > b) {
        return a;
    } else if (a==b) {
        return b;
    } else {
        return b;
    }
}

int main() {
    int x = 5;
    int y = 7;
    int z;
    z = max(x, y);
    if(z > 10)
        return 1;
    return 0;
}
