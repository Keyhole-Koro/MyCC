//typedef int MyInt;
//typedef char MyChar;

void print_char(char c) {
}

char hello_string() {
    return "hello";
}

int add(int a, int b) {
    return a + b;
}

int fact(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * fact(n - 1);
}

int main() {
    int x = 1;
    //char y = 'z';
    int z = add(x, 2);
    char ch = y;
    char str = hello_string();

    print_char(ch);
    do_nothing();

    if (z > 2) {
        while (z > 0) {
            z = z - 1;
        }
        return z;
    } else if (z == 2) {
        for (int i = 0; i < 5; i = i + 1) {
            x = x + i;
        }
        return x;
    } else {
        z = z + 10;
    }

    return 0;
}
